-- Database initialization script for Phonebook App

-- Create the phonebook database if it doesn't exist
-- Note: This will be run automatically by PostgreSQL on first run

-- The schema will be created by the application itself using SQLAlchemy
-- This is just a placeholder for any additional initialization if needed

-- Create indexes for better performance (these will be created by the app as well)
-- CREATE INDEX IF NOT EXISTS idx_contacts_user_id ON contacts(user_id);
-- CREATE INDEX IF NOT EXISTS idx_contacts_name ON contacts(name);
-- CREATE INDEX IF NOT EXISTS idx_contacts_phone_number ON contacts(phone_number);
-- CREATE INDEX IF NOT EXISTS idx_contacts_email ON contacts(email);